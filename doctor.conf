proxy_http_version 1.1;

location /api/ { proxy_pass http://doctor-be.bavs.space:8080; }

location / {
            try_files $uri $uri/ /index.html;
        }

location /health {
  stub_status on;
  access_log off;
}
